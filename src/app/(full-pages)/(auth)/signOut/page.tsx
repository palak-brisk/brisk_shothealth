import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "BriskMD | Signout",
  description: "BriskMD | Signout",
};

const signOut = () => {
  return <div>signOut</div>;
};

export default signOut;
